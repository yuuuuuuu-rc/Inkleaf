{
  "name": "inkleaf",
  "version": "0.6.0",
  "private": true,
  "description": "A local-first EPUB and PDF reader for AI-assisted reading, translation, and interactive notes",
  "scripts": {
    "start": "node server.cjs --open",
    "start:server": "node server.cjs",
    "check": "node --check server.cjs && node --check companion.cjs && node --check dist/web-bridge.js && node --check dist/companion-ui.js && node --check dist/pdf-reader.js && node --check dist/assets/index-Cni3k_A9.js && node --check tools/smoke-test.cjs",
    "test": "npm run check && node tools/smoke-test.cjs && node --test tools/companion.test.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
