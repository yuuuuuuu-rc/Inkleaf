{
  "name": "inkleaf",
  "version": "0.4.1",
  "private": true,
  "description": "A local-first EPUB reader for AI-assisted reading, translation, and interactive notes",
  "scripts": {
    "start": "node server.cjs --open",
    "start:server": "node server.cjs",
    "check": "node --check server.cjs && node --check dist/web-bridge.js && node --check tools/smoke-test.cjs",
    "test": "npm run check && node tools/smoke-test.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
